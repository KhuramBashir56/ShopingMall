@props(['for'])
<input type="file" name="{{ $for }}" id = "{{ $for }}" class="block w-full p-[5px] mt-1 text-md cursor-pointer rounded-md focus:border-orange-500 focus:ring-orange-500 form-input" {{ $attributes }} />
